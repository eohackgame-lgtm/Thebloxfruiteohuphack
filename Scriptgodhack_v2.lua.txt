local VirtualInputManager = game:GetService("VirtualInputManager")
local CollectionService = game:GetService("CollectionService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TeleportService = game:GetService("TeleportService")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local Player = Players.LocalPlayer

-- 檢查是否成功獲取本地玩家
if not Player then
    warn("無法找到玩家。請確保您在客戶端運行腳本。")
    return
end

local PlayerGui = Player:WaitForChild("PlayerGui")
local ESPPanel = nil

-- 顯示通知（加載時自動顯示）
game.StarterGui:SetCore("SendNotification", {
    Title = "eoHub",
    Text = "成功運行，測試階段請別亂用",
    Duration = 999
})

-- 創建主屏幕GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "SimpleGUI"
ScreenGui.Parent = PlayerGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- ========== 密碼保護系統開始 ==========
local password = "eogame1223"  -- 設置密碼 <--- 這裡是密碼設置位置

-- 創建密碼驗證GUI
local PasswordScreenGui = Instance.new("ScreenGui")
PasswordScreenGui.Name = "PasswordScreenGui"
PasswordScreenGui.Parent = PlayerGui
PasswordScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
PasswordScreenGui.ResetOnSpawn = false

-- 創建黑色背景遮罩
local BlackOverlay = Instance.new("Frame")
BlackOverlay.Name = "BlackOverlay"
BlackOverlay.Size = UDim2.new(1, 0, 1, 0)
BlackOverlay.Position = UDim2.new(0, 0, 0, 0)
BlackOverlay.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
BlackOverlay.BackgroundTransparency = 0.1
BlackOverlay.BorderSizePixel = 0
BlackOverlay.Parent = PasswordScreenGui

-- 創建白色長方形密碼按鈕
local PasswordButton = Instance.new("TextButton")
PasswordButton.Name = "PasswordButton"
PasswordButton.Size = UDim2.new(0, 300, 0, 50)
PasswordButton.Position = UDim2.new(0.5, -150, 0.5, -25)
PasswordButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
PasswordButton.TextColor3 = Color3.fromRGB(0, 0, 0)
PasswordButton.Text = "點擊輸入密碼"
PasswordButton.Font = Enum.Font.Gotham
PasswordButton.TextScaled = true
PasswordButton.Parent = BlackOverlay

-- 創建密碼輸入框（初始隱藏）
local PasswordInputFrame = Instance.new("Frame")
PasswordInputFrame.Name = "PasswordInputFrame"
PasswordInputFrame.Size = UDim2.new(0, 350, 0, 200)
PasswordInputFrame.Position = UDim2.new(0.5, -175, 0.5, -100)
PasswordInputFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
PasswordInputFrame.BorderSizePixel = 0
PasswordInputFrame.Visible = false
PasswordInputFrame.Parent = BlackOverlay

local PasswordInputCorner = Instance.new("UICorner")
PasswordInputCorner.CornerRadius = UDim.new(0.05, 0)
PasswordInputCorner.Parent = PasswordInputFrame

-- 密碼輸入標題
local PasswordTitle = Instance.new("TextLabel")
PasswordTitle.Name = "PasswordTitle"
PasswordTitle.Size = UDim2.new(0, 310, 0, 40)
PasswordTitle.Position = UDim2.new(0, 20, 0, 20)
PasswordTitle.BackgroundTransparency = 1
PasswordTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
PasswordTitle.Text = "請輸入密碼以使用 eoHub"
PasswordTitle.Font = Enum.Font.GothamBold
PasswordTitle.TextScaled = true
PasswordTitle.Parent = PasswordInputFrame

-- 密碼輸入框
local PasswordTextBox = Instance.new("TextBox")
PasswordTextBox.Name = "PasswordTextBox"
PasswordTextBox.Size = UDim2.new(0, 310, 0, 40)
PasswordTextBox.Position = UDim2.new(0, 20, 0, 70)
PasswordTextBox.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
PasswordTextBox.TextColor3 = Color3.fromRGB(255, 255, 255)
PasswordTextBox.Font = Enum.Font.Gotham
PasswordTextBox.TextScaled = true
PasswordTextBox.PlaceholderText = "輸入密碼..."
PasswordTextBox.Text = ""
PasswordTextBox.Parent = PasswordInputFrame

local PasswordTextBoxCorner = Instance.new("UICorner")
PasswordTextBoxCorner.CornerRadius = UDim.new(0.05, 0)
PasswordTextBoxCorner.Parent = PasswordTextBox

-- 確認按鈕
local ConfirmButton = Instance.new("TextButton")
ConfirmButton.Name = "ConfirmButton"
ConfirmButton.Size = UDim2.new(0, 150, 0, 40)
ConfirmButton.Position = UDim2.new(0, 20, 0, 130)
ConfirmButton.BackgroundColor3 = Color3.fromRGB(80, 200, 120)
ConfirmButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ConfirmButton.Text = "確認"
ConfirmButton.Font = Enum.Font.Gotham
ConfirmButton.TextScaled = true
ConfirmButton.Parent = PasswordInputFrame

local ConfirmButtonCorner = Instance.new("UICorner")
ConfirmButtonCorner.CornerRadius = UDim.new(0.05, 0)
ConfirmButtonCorner.Parent = ConfirmButton

-- 錯誤信息顯示
local ErrorLabel = Instance.new("TextLabel")
ErrorLabel.Name = "ErrorLabel"
ErrorLabel.Size = UDim2.new(0, 310, 0, 30)
ErrorLabel.Position = UDim2.new(0, 20, 0, 110)
ErrorLabel.BackgroundTransparency = 1
ErrorLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
ErrorLabel.Text = ""
ErrorLabel.Font = Enum.Font.Gotham
ErrorLabel.TextScaled = true
ErrorLabel.Visible = false
ErrorLabel.Parent = PasswordInputFrame

-- 密碼驗證功能
local function verifyPassword()
    local inputPassword = PasswordTextBox.Text
    
    if inputPassword == password then
        -- 密碼正確，隱藏密碼保護界面，顯示主GUI圖標
        PasswordScreenGui:Destroy()
        ScreenGui.Enabled = true
        
        game.StarterGui:SetCore("SendNotification", {
            Title = "eoHub",
            Text = "密碼正確！功能已解鎖",
            Duration = 3
        })
    else
        -- 密碼錯誤
        ErrorLabel.Text = "密碼錯誤！請重新輸入"
        ErrorLabel.Visible = true
        PasswordTextBox.Text = ""
        
        -- 錯誤提示後3秒隱藏錯誤信息
        task.wait(3)
        ErrorLabel.Visible = false
    end
end

-- 白色按鈕點擊事件：顯示密碼輸入框
PasswordButton.MouseButton1Click:Connect(function()
    PasswordInputFrame.Visible = true
    PasswordButton.Visible = false
end)

-- 確認按鈕點擊事件
ConfirmButton.MouseButton1Click:Connect(function()
    verifyPassword()
end)

-- 回車鍵提交密碼
PasswordTextBox.FocusLost:Connect(function(enterPressed)
    if enterPressed then
        verifyPassword()
    end
end)

-- 初始隱藏主GUI，直到密碼驗證通過
ScreenGui.Enabled = false
-- ========== 密碼保護系統結束 ==========

-- 創建圖標按鈕
local IconButton = Instance.new("ImageButton")
IconButton.Name = "IconButton"
IconButton.Size = UDim2.new(0, 60, 0, 60)
IconButton.Position = UDim2.new(0, 20, 0, 20)
IconButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
IconButton.BackgroundTransparency = 0.5
IconButton.Image = "rbxassetid://138136991120099"
IconButton.ScaleType = Enum.ScaleType.Fit
IconButton.Parent = ScreenGui

-- 創建圓角效果
local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0.2, 0)
UICorner.Parent = IconButton

-- 創建主面板
local MainPanel = Instance.new("Frame")
MainPanel.Name = "MainPanel"
MainPanel.Size = UDim2.new(0, 300, 0, 350)
MainPanel.Position = UDim2.new(0, 90, 0, 20)
MainPanel.BackgroundColor3 = Color3.fromRGB(0,0,0)
MainPanel.BackgroundTransparency = 0.1
MainPanel.Visible = false
MainPanel.Parent = ScreenGui

local MainPanelCorner = Instance.new("UICorner")
MainPanelCorner.CornerRadius = UDim.new(0.05, 0)
MainPanelCorner.Parent = MainPanel

-- 主面板標題
local MainPanelTitle = Instance.new("TextLabel")
MainPanelTitle.Size = UDim2.new(1, 0, 0, 35)
MainPanelTitle.Position = UDim2.new(0, 0, 0, 0)
MainPanelTitle.BackgroundTransparency = 1
MainPanelTitle.Text = "eoHub 主選單"
MainPanelTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
MainPanelTitle.TextScaled = true
MainPanelTitle.Font = Enum.Font.GothamBold
MainPanelTitle.Parent = MainPanel

-- 可上下滑動的 ScrollingFrame
local MainScroll = Instance.new("ScrollingFrame")
MainScroll.Name = "MainScroll"
MainScroll.Size = UDim2.new(1, 0, 1, -38)
MainScroll.Position = UDim2.new(0, 0, 0, 38)
MainScroll.BackgroundTransparency = 1
MainScroll.ScrollBarThickness = 4
MainScroll.ScrollBarImageColor3 = Color3.fromRGB(150, 150, 150)
MainScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
MainScroll.AutomaticCanvasSize = Enum.AutomaticSize.None
MainScroll.ScrollingDirection = Enum.ScrollingDirection.Y
MainScroll.ScrollingEnabled = true
MainScroll.Active = true
MainScroll.ClipsDescendants = true
MainScroll.ElasticBehavior = Enum.ElasticBehavior.WhenScrollable
MainScroll.Parent = MainPanel

local MainListLayout = Instance.new("UIListLayout")
MainListLayout.SortOrder = Enum.SortOrder.LayoutOrder
MainListLayout.Padding = UDim.new(0, 8)
MainListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
MainListLayout.Parent = MainScroll

local MainScrollPadding = Instance.new("UIPadding")
MainScrollPadding.PaddingTop = UDim.new(0, 6)
MainScrollPadding.PaddingBottom = UDim.new(0, 12)
MainScrollPadding.Parent = MainScroll

local function updateMainScrollCanvas()
    local contentHeight = MainListLayout.AbsoluteContentSize.Y + MainScrollPadding.PaddingTop.Offset + MainScrollPadding.PaddingBottom.Offset
    MainScroll.CanvasSize = UDim2.new(0, 0, 0, contentHeight)
end
MainListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(updateMainScrollCanvas)
task.defer(updateMainScrollCanvas)

-- 建立按鈕輔助函數
local function makeMainButton(text, color)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 260, 0, 42)
    btn.BackgroundColor3 = color
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextScaled = true
    btn.Font = Enum.Font.Gotham
    btn.Active = true
    btn.Selectable = true
    btn.Parent = MainScroll
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
    return btn
end

-- 按鈕（由上到下）
local teleportButton   = makeMainButton("自動海上飛行",  Color3.fromRGB(80, 200, 120))
local stopButton       = makeMainButton("停止移動",      Color3.fromRGB(200, 80, 80))
local flyToTikiButton  = makeMainButton("飛到 Tiki 島",  Color3.fromRGB(180, 100, 220))
local espFruitButton   = makeMainButton("ESP 果實",      Color3.fromRGB(220, 130, 50))
local speedButton      = makeMainButton("速度設置",      Color3.fromRGB(100, 180, 255))
local autoBattleButton = makeMainButton("自動攻擊設置",  Color3.fromRGB(220, 150, 80))
local espButton        = makeMainButton("ESP 玩家設定", Color3.fromRGB(180, 80, 200))
local infoButton       = makeMainButton("顯示信息",      Color3.fromRGB(80, 120, 200))
local flySettingsButton = makeMainButton("飛行設置",      Color3.fromRGB(150, 100, 255))

-- 創建信息頁面面板
local InfoPanel = Instance.new("Frame")
InfoPanel.Name = "InfoPanel"
InfoPanel.Size = UDim2.new(0, 300, 0, 200)
InfoPanel.Position = UDim2.new(0, 90, 0, 20)
InfoPanel.BackgroundColor3 = Color3.fromRGB(0,0,0)
InfoPanel.BackgroundTransparency = 0.1
InfoPanel.Visible = false
InfoPanel.Parent = ScreenGui

local InfoPanelCorner = Instance.new("UICorner")
InfoPanelCorner.CornerRadius = UDim.new(0.05, 0)
InfoPanelCorner.Parent = InfoPanel

-- ========== 自動戰鬥系統設置開始 ==========
local FastAttackSettings = {
    AutoClick = false,
    ClickDelay = 0,
    Team = "Marines",
    Distance = 150
}
-- ========== 自動戰鬥系統設置結束 ==========

-- 在信息页面添加返回按钮
local backButton = Instance.new("TextButton")
backButton.Name = "BackButton"
backButton.Size = UDim2.new(0, 150, 0, 40)
backButton.Position = UDim2.new(0, 20, 0, 120)
backButton.BackgroundColor3 = Color3.fromRGB(200, 120, 80)
backButton.Text = "返回主頁"
backButton.TextColor3 = Color3.fromRGB(255, 255, 255)
backButton.TextScaled = true
backButton.Parent = InfoPanel

-- 在信息页面添加信息文本
local infoText = Instance.new("TextLabel")
infoText.Name = "InfoText"
infoText.Size = UDim2.new(0, 260, 0, 100)
infoText.Position = UDim2.new(0, 20, 0, 20)
infoText.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
infoText.BackgroundTransparency = 0.3
infoText.Text = "eoHub 信息頁面\n\n版本: E1.5\n功能: 自動海上飛行、自動到司法\n作者: eoHub團隊"
infoText.TextColor3 = Color3.fromRGB(255, 255, 255)
infoText.TextScaled = true
infoText.Font = Enum.Font.Gotham
infoText.Parent = InfoPanel

-- 为所有按钮添加圆角
local teleportCorner = Instance.new("UICorner")
teleportCorner.CornerRadius = UDim.new(0, 8)
teleportCorner.Parent = teleportButton

local stopCorner = Instance.new("UICorner")
stopCorner.CornerRadius = UDim.new(0, 8)
stopCorner.Parent = stopButton

local infoCorner = Instance.new("UICorner")
infoCorner.CornerRadius = UDim.new(0, 8)
infoCorner.Parent = infoButton

local flyToTikiCorner = Instance.new("UICorner")
flyToTikiCorner.CornerRadius = UDim.new(0, 8)
flyToTikiCorner.Parent = flyToTikiButton

local backCorner = Instance.new("UICorner")
backCorner.CornerRadius = UDim.new(0, 8)
backCorner.Parent = backButton

local infoTextCorner = Instance.new("UICorner")
infoTextCorner.CornerRadius = UDim.new(0, 8)
infoTextCorner.Parent = infoText

-- 創建自動戰鬥面板
local BattlePanel = Instance.new("Frame")
BattlePanel.Name = "BattlePanel"
BattlePanel.Size = UDim2.new(0, 300, 0, 285)
BattlePanel.Position = UDim2.new(0, 90, 0, 20)
BattlePanel.BackgroundColor3 = Color3.fromRGB(0,0,0)
BattlePanel.BackgroundTransparency = 0.1
BattlePanel.Visible = false
BattlePanel.Parent = ScreenGui

local BattlePanelCorner = Instance.new("UICorner")
BattlePanelCorner.CornerRadius = UDim.new(0.05, 0)
BattlePanelCorner.Parent = BattlePanel

-- 標題
local battleTitle = Instance.new("TextLabel")
battleTitle.Name = "BattleTitle"
battleTitle.Size = UDim2.new(0, 260, 0, 35)
battleTitle.Position = UDim2.new(0, 20, 0, 10)
battleTitle.BackgroundTransparency = 1
battleTitle.Text = "自動戰鬥設置"
battleTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
battleTitle.TextScaled = true
battleTitle.Font = Enum.Font.GothamBold
battleTitle.Parent = BattlePanel

-- 重生成海盜按鈕
local respawnPirateButton = Instance.new("TextButton")
respawnPirateButton.Name = "RespawnPirateButton"
respawnPirateButton.Size = UDim2.new(0, 260, 0, 40)
respawnPirateButton.Position = UDim2.new(0, 20, 0, 55)
respawnPirateButton.BackgroundColor3 = Color3.fromRGB(200, 60, 60)
respawnPirateButton.Text = "重生成海盜"
respawnPirateButton.TextColor3 = Color3.fromRGB(255, 255, 255)
respawnPirateButton.TextScaled = true
respawnPirateButton.Parent = BattlePanel

local respawnPirateCorner = Instance.new("UICorner")
respawnPirateCorner.CornerRadius = UDim.new(0, 8)
respawnPirateCorner.Parent = respawnPirateButton

-- 重生成海軍按鈕
local respawnMarineButton = Instance.new("TextButton")
respawnMarineButton.Name = "RespawnMarineButton"
respawnMarineButton.Size = UDim2.new(0, 260, 0, 40)
respawnMarineButton.Position = UDim2.new(0, 20, 0, 103)
respawnMarineButton.BackgroundColor3 = Color3.fromRGB(60, 100, 200)
respawnMarineButton.Text = "重生成海軍"
respawnMarineButton.TextColor3 = Color3.fromRGB(255, 255, 255)
respawnMarineButton.TextScaled = true
respawnMarineButton.Parent = BattlePanel

local respawnMarineCorner = Instance.new("UICorner")
respawnMarineCorner.CornerRadius = UDim.new(0, 8)
respawnMarineCorner.Parent = respawnMarineButton

-- 自動戰鬥開關按鈕（在海軍下面）
local toggleAutoBattleButton = Instance.new("TextButton")
toggleAutoBattleButton.Name = "ToggleAutoBattleButton"
toggleAutoBattleButton.Size = UDim2.new(0, 260, 0, 40)
toggleAutoBattleButton.Position = UDim2.new(0, 20, 0, 151)
toggleAutoBattleButton.BackgroundColor3 = Color3.fromRGB(200, 80, 80)
toggleAutoBattleButton.Text = "關閉自動攻擊"
toggleAutoBattleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
toggleAutoBattleButton.TextScaled = true
toggleAutoBattleButton.Parent = BattlePanel

local toggleAutoBattleCorner = Instance.new("UICorner")
toggleAutoBattleCorner.CornerRadius = UDim.new(0, 8)
toggleAutoBattleCorner.Parent = toggleAutoBattleButton

-- 返回按鈕
local battleBackButton = Instance.new("TextButton")
battleBackButton.Name = "BattleBackButton"
battleBackButton.Size = UDim2.new(0, 260, 0, 40)
battleBackButton.Position = UDim2.new(0, 20, 0, 230)
battleBackButton.BackgroundColor3 = Color3.fromRGB(200, 120, 80)
battleBackButton.Text = "返回主頁"
battleBackButton.TextColor3 = Color3.fromRGB(255, 255, 255)
battleBackButton.TextScaled = true
battleBackButton.Parent = BattlePanel

local battleBackCorner = Instance.new("UICorner")
battleBackCorner.CornerRadius = UDim.new(0, 8)
battleBackCorner.Parent = battleBackButton

-- 创建速度设置面板
local SpeedPanel = Instance.new("Frame")
SpeedPanel.Name = "SpeedPanel"
SpeedPanel.Size = UDim2.new(0, 300, 0, 230)
SpeedPanel.Position = UDim2.new(0, 90, 0, 20)
SpeedPanel.BackgroundColor3 = Color3.fromRGB(0,0,0)
SpeedPanel.BackgroundTransparency = 0.1
SpeedPanel.Visible = false
SpeedPanel.Parent = ScreenGui

local SpeedPanelCorner = Instance.new("UICorner")
SpeedPanelCorner.CornerRadius = UDim.new(0.05, 0)
SpeedPanelCorner.Parent = SpeedPanel

-- 標題
local speedTitle = Instance.new("TextLabel")
speedTitle.Name = "SpeedTitle"
speedTitle.Size = UDim2.new(0, 260, 0, 35)
speedTitle.Position = UDim2.new(0, 20, 0, 10)
speedTitle.BackgroundTransparency = 1
speedTitle.Text = "速度設置"
speedTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
speedTitle.TextScaled = true
speedTitle.Font = Enum.Font.GothamBold
speedTitle.Parent = SpeedPanel

-- 速度輸入框
local speedTextBox = Instance.new("TextBox")
speedTextBox.Name = "SpeedTextBox"
speedTextBox.Size = UDim2.new(0, 260, 0, 40)
speedTextBox.Position = UDim2.new(0, 20, 0, 52)
speedTextBox.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
speedTextBox.TextColor3 = Color3.fromRGB(255, 255, 255)
speedTextBox.Font = Enum.Font.Gotham
speedTextBox.TextScaled = true
speedTextBox.PlaceholderText = "輸入移動速度數字"
speedTextBox.Text = ""
speedTextBox.ClearTextOnFocus = false
speedTextBox.Parent = SpeedPanel

local speedTextBoxCorner = Instance.new("UICorner")
speedTextBoxCorner.CornerRadius = UDim.new(0, 8)
speedTextBoxCorner.Parent = speedTextBox

-- 確認按鈕
local confirmSpeedButton = Instance.new("TextButton")
confirmSpeedButton.Name = "ConfirmSpeedButton"
confirmSpeedButton.Size = UDim2.new(0, 260, 0, 40)
confirmSpeedButton.Position = UDim2.new(0, 20, 0, 100)
confirmSpeedButton.BackgroundColor3 = Color3.fromRGB(80, 130, 200)
confirmSpeedButton.Text = "確認"
confirmSpeedButton.TextColor3 = Color3.fromRGB(255, 255, 255)
confirmSpeedButton.TextScaled = true
confirmSpeedButton.Parent = SpeedPanel

local confirmSpeedCorner = Instance.new("UICorner")
confirmSpeedCorner.CornerRadius = UDim.new(0, 8)
confirmSpeedCorner.Parent = confirmSpeedButton

-- 目前速度顯示（確認後才出現）
local speedStatusLabel = Instance.new("TextLabel")
speedStatusLabel.Name = "SpeedStatusLabel"
speedStatusLabel.Size = UDim2.new(0, 260, 0, 28)
speedStatusLabel.Position = UDim2.new(0, 20, 0, 148)
speedStatusLabel.BackgroundTransparency = 1
speedStatusLabel.Text = ""
speedStatusLabel.TextColor3 = Color3.fromRGB(100, 220, 100)
speedStatusLabel.TextScaled = true
speedStatusLabel.Font = Enum.Font.Gotham
speedStatusLabel.Parent = SpeedPanel

-- 返回按鈕
local speedBackButton = Instance.new("TextButton")
speedBackButton.Name = "SpeedBackButton"
speedBackButton.Size = UDim2.new(0, 260, 0, 40)
speedBackButton.Position = UDim2.new(0, 20, 0, 182)
speedBackButton.BackgroundColor3 = Color3.fromRGB(200, 120, 80)
speedBackButton.Text = "返回主頁"
speedBackButton.TextColor3 = Color3.fromRGB(255, 255, 255)
speedBackButton.TextScaled = true
speedBackButton.Parent = SpeedPanel

local speedBackCorner = Instance.new("UICorner")
speedBackCorner.CornerRadius = UDim.new(0, 8)
speedBackCorner.Parent = speedBackButton

-- 新增飛行功能相關的 UI 組件和邏輯

-- 飛行控制變量
local isFlying = false
local flySpeed = 50 -- 預設飛行速度 (最高400)
local flyConnection = nil

-- 創建飛行設置面板
local FlyPanel = Instance.new("Frame")
FlyPanel.Name = "FlyPanel"
FlyPanel.Size = UDim2.new(0, 300, 0, 200)
FlyPanel.Position = UDim2.new(0, 90, 0, 20)
FlyPanel.BackgroundColor3 = Color3.fromRGB(0,0,0)
FlyPanel.BackgroundTransparency = 0.1
FlyPanel.Visible = false
FlyPanel.Parent = ScreenGui

local FlyPanelCorner = Instance.new("UICorner")
FlyPanelCorner.CornerRadius = UDim.new(0.05, 0)
FlyPanelCorner.Parent = FlyPanel

-- 飛行面板標題
local flyTitle = Instance.new("TextLabel")
flyTitle.Name = "FlyTitle"
flyTitle.Size = UDim2.new(0, 260, 0, 35)
flyTitle.Position = UDim2.new(0, 20, 0, 10)
flyTitle.BackgroundTransparency = 1
flyTitle.Text = "飛行設置"
flyTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
flyTitle.TextScaled = true
flyTitle.Font = Enum.Font.GothamBold
flyTitle.Parent = FlyPanel

-- 開啟/關閉飛行按鈕
local toggleFlyButton = Instance.new("TextButton")
toggleFlyButton.Name = "ToggleFlyButton"
toggleFlyButton.Size = UDim2.new(0, 260, 0, 40)
toggleFlyButton.Position = UDim2.new(0, 20, 0, 55)
toggleFlyButton.BackgroundColor3 = Color3.fromRGB(200, 80, 80) -- 預設為關閉紅色
toggleFlyButton.Text = "開啟自由飛行"
toggleFlyButton.TextColor3 = Color3.fromRGB(255, 255, 255)
toggleFlyButton.TextScaled = true
toggleFlyButton.Font = Enum.Font.Gotham
toggleFlyButton.Parent = FlyPanel

local toggleFlyCorner = Instance.new("UICorner")
toggleFlyCorner.CornerRadius = UDim.new(0, 8)
toggleFlyCorner.Parent = toggleFlyButton

-- 速度滑動條標籤
local flySpeedLabel = Instance.new("TextLabel")
flySpeedLabel.Name = "FlySpeedLabel"
flySpeedLabel.Size = UDim2.new(0, 260, 0, 20)
flySpeedLabel.Position = UDim2.new(0, 20, 0, 105)
flySpeedLabel.BackgroundTransparency = 1
flySpeedLabel.Text = "飛行速度: " .. flySpeed
flySpeedLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
flySpeedLabel.TextScaled = true
flySpeedLabel.Font = Enum.Font.Gotham
flySpeedLabel.Parent = FlyPanel

-- 速度滑動條 (使用 Frame 和 TextButton 模擬)
local flySpeedSliderFrame = Instance.new("Frame")
flySpeedSliderFrame.Name = "FlySpeedSliderFrame"
flySpeedSliderFrame.Size = UDim2.new(0, 260, 0, 10)
flySpeedSliderFrame.Position = UDim2.new(0, 20, 0, 130)
flySpeedSliderFrame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
flySpeedSliderFrame.Parent = FlyPanel

local flySpeedSlider = Instance.new("TextButton")
flySpeedSlider.Name = "FlySpeedSlider"
flySpeedSlider.Size = UDim2.new(0.125, 0, 1, 0) -- 初始值為 50% (50)
flySpeedSlider.Position = UDim2.new(0, 0, 0, 0)
flySpeedSlider.BackgroundColor3 = Color3.fromRGB(80, 200, 120)
flySpeedSlider.Text = ""
flySpeedSlider.Parent = flySpeedSliderFrame

local flySpeedSliderCorner = Instance.new("UICorner")
flySpeedSliderCorner.CornerRadius = UDim.new(0, 5)
flySpeedSliderCorner.Parent = flySpeedSliderFrame

local flySpeedThumb = Instance.new("Frame")
flySpeedThumb.Name = "FlySpeedThumb"
flySpeedThumb.Size = UDim2.new(0, 10, 0, 20)
flySpeedThumb.Position = UDim2.new(0.125, -5, -0.5, 0)
flySpeedThumb.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
flySpeedThumb.BorderSizePixel = 0
flySpeedThumb.Parent = flySpeedSliderFrame

local flySpeedThumbCorner = Instance.new("UICorner")
flySpeedThumbCorner.CornerRadius = UDim.new(0, 5)
flySpeedThumbCorner.Parent = flySpeedThumb

-- 飛行面板返回按鈕
local flyBackButton = Instance.new("TextButton")
flyBackButton.Name = "FlyBackButton"
flyBackButton.Size = UDim2.new(0, 150, 0, 40)
flyBackButton.Position = UDim2.new(0, 20, 0, 155)
flyBackButton.BackgroundColor3 = Color3.fromRGB(200, 120, 80)
flyBackButton.Text = "返回主頁"
flyBackButton.TextColor3 = Color3.fromRGB(255, 255, 255)
flyBackButton.TextScaled = true
flyBackButton.Font = Enum.Font.Gotham
flyBackButton.Parent = FlyPanel

local flyBackCorner = Instance.new("UICorner")
flyBackCorner.CornerRadius = UDim.new(0, 8)
flyBackCorner.Parent = flyBackButton



-- 船體偵測與移動輔助：坐在船上時，優先對座位所在的船模型進行位移。
local function getSeatedVessel(character)
    local humanoid = character and character:FindFirstChildOfClass("Humanoid")
    local seat = humanoid and humanoid.SeatPart
    if not seat or not seat:IsA("BasePart") then return nil end

    local vessel = nil
    local current = seat.Parent
    while current and current ~= workspace do
        if current:IsA("Model") then vessel = current end
        current = current.Parent
    end
    return vessel or seat
end

local function getContainerPivot(container)
    if container:IsA("Model") then return container:GetPivot() end
    return container.CFrame
end

local function pivotContainer(container, targetCFrame)
    if container:IsA("Model") then
        container:PivotTo(targetCFrame)
    else
        container.CFrame = targetCFrame
    end
end

local function getObstacleLift(container, fromPosition, movement)
    local horizontal = Vector3.new(movement.X, 0, movement.Z)
    if horizontal.Magnitude < 0.01 then return Vector3.new(0, 0, 0) end

    local params = RaycastParams.new()
    params.FilterType = Enum.RaycastFilterType.Exclude
    params.FilterDescendantsInstances = {container, Player.Character}
    params.IgnoreWater = true
    local rayOrigin = fromPosition + Vector3.new(0, 8, 0)
    local hit = workspace:Raycast(rayOrigin, horizontal.Unit * math.min(horizontal.Magnitude + 20, 80), params)
    if hit then
        -- 遇到島嶼／石頭時先提高高度，避免船首卡住；不改變水平方向。
        local neededHeight = math.clamp(hit.Position.Y - fromPosition.Y + 25, 25, 90)
        return Vector3.new(0, neededHeight, 0)
    end
    return Vector3.new(0, 0, 0)
end

local function moveVesselOrCharacter(character, root, movement, facingDirection)
    local vessel = getSeatedVessel(character)
    if vessel then
        local pivot = getContainerPivot(vessel)
        local adjustedMovement = movement + getObstacleLift(vessel, pivot.Position, movement)
        local nextPosition = pivot.Position + adjustedMovement
        local flatFacing = Vector3.new(facingDirection.X, 0, facingDirection.Z)
        local targetCFrame
        if flatFacing.Magnitude > 0.01 then
            targetCFrame = CFrame.lookAt(nextPosition, nextPosition + flatFacing.Unit)
        else
            targetCFrame = CFrame.new(nextPosition) * (pivot - pivot.Position)
        end
        pivotContainer(vessel, targetCFrame)
        return true
    end

    local nextPosition = root.Position + movement
    local flatFacing = Vector3.new(facingDirection.X, 0, facingDirection.Z)
    if flatFacing.Magnitude > 0.01 then
        root.CFrame = CFrame.lookAt(nextPosition, nextPosition + flatFacing.Unit)
    else
        root.CFrame = CFrame.new(nextPosition)
    end
    return false
end

-- 自動海上飛行：坐船時移動整艘船；未坐船時才移動角色。
local isMoving = false
local moveConnection = nil
local SEA_FLY_SPEED = 300
local SEA_FLY_HEIGHT = 180

local function stopSeaFly(showNotification)
    isMoving = false
    if moveConnection then
        moveConnection:Disconnect()
        moveConnection = nil
    end
    if showNotification then
        game.StarterGui:SetCore("SendNotification", {
            Title = "已停止",
            Text = "自動海上飛行已停止",
            Duration = 3
        })
    end
end

local function startSeaFly()
    local character = Player.Character
    local root = character and character:FindFirstChild("HumanoidRootPart")
    if not root then
        game.StarterGui:SetCore("SendNotification", {
            Title = "自動海上飛行",
            Text = "找不到角色，請重生後再試",
            Duration = 3
        })
        return
    end

    stopSeaFly(false)
    isMoving = true

    moveConnection = RunService.Heartbeat:Connect(function(deltaTime)
        if not isMoving then
            stopSeaFly(false)
            return
        end
        local currentCharacter = Player.Character
        local currentRoot = currentCharacter and currentCharacter:FindFirstChild("HumanoidRootPart")
        if not currentRoot then
            stopSeaFly(false)
            return
        end

        local vessel = getSeatedVessel(currentCharacter)
        if vessel then
            local pivot = getContainerPivot(vessel)
            local nextPosition = Vector3.new(pivot.Position.X - SEA_FLY_SPEED * deltaTime, SEA_FLY_HEIGHT, pivot.Position.Z)
            local target = CFrame.new(nextPosition) * CFrame.Angles(0, math.rad(90), 0)
            -- PivotTo 直接移動船模型，避免角色脫離船體及船被石頭阻擋。
            pivotContainer(vessel, target)
        else
            local position = currentRoot.Position
            local nextPosition = Vector3.new(position.X - SEA_FLY_SPEED * deltaTime, SEA_FLY_HEIGHT, position.Z)
            currentRoot.CFrame = CFrame.new(nextPosition) * CFrame.Angles(0, math.rad(90), 0)
        end
    end)

    game.StarterGui:SetCore("SendNotification", {
        Title = "自動海上飛行",
        Text = getSeatedVessel(character) and "已啟動：優先移動船體" or "已啟動：目前未坐船，移動角色",
        Duration = 3
    })
end

teleportButton.MouseButton1Click:Connect(startSeaFly)
stopButton.MouseButton1Click:Connect(function() stopSeaFly(true) end)
Player.CharacterAdded:Connect(function() stopSeaFly(false) end)

-- 信息按钮功能
infoButton.MouseButton1Click:Connect(function()
    MainPanel.Visible = false
    InfoPanel.Visible = true
    BattlePanel.Visible = false
    SpeedPanel.Visible = false
    FlyPanel.Visible = false
    updatePanelStatus()
end)





-- 返回按钮功能
backButton.MouseButton1Click:Connect(function()
    InfoPanel.Visible = false
    MainPanel.Visible = true
    BattlePanel.Visible = false
    SpeedPanel.Visible = false
    FlyPanel.Visible = false
    updatePanelStatus()
end)

-- 自动战斗按钮点击事件
autoBattleButton.MouseButton1Click:Connect(function()
    MainPanel.Visible = false
    InfoPanel.Visible = false
    BattlePanel.Visible = true
    SpeedPanel.Visible = false
    FlyPanel.Visible = false
    updatePanelStatus()
end)

-- 战斗面板返回按钮事件
battleBackButton.MouseButton1Click:Connect(function()
    BattlePanel.Visible = false
    MainPanel.Visible = true
    InfoPanel.Visible = false
    SpeedPanel.Visible = false
    FlyPanel.Visible = false
    updatePanelStatus()
end)

-- 速度设置按钮点击事件
speedButton.MouseButton1Click:Connect(function()
    MainPanel.Visible = false
    InfoPanel.Visible = false
    BattlePanel.Visible = false
    SpeedPanel.Visible = true
    updatePanelStatus()
end)

-- 速度面板返回按钮事件
speedBackButton.MouseButton1Click:Connect(function()
    SpeedPanel.Visible = false
    MainPanel.Visible = true
    InfoPanel.Visible = false
    BattlePanel.Visible = false
    updatePanelStatus()
end)

-- ========== 自动战斗功能代码开始 ==========
local _G = _G or getgenv()
_G.FastAttack = true

local function SafeWaitForChild(parent, childName)
    local success, result = pcall(function()
        return parent:WaitForChild(childName)
    end)
    if not success or not result then
        warn("找不到: " .. childName)
    end
    return result
end

local function WaitChilds(path, ...)
    local last = path
    for _, child in {...} do
        last = last:FindFirstChild(child) or SafeWaitForChild(last, child)
        if not last then break end
    end
    return last
end

local FastAttackModule = {}

FastAttackModule.FastAttack = (function()
    if getgenv().rz_FastAttack then
        return getgenv().rz_FastAttack
    end

    local FastAttack = {
        Distance = FastAttackSettings.Distance,
        attackMobs = true,
        attackPlayers = true,
        Equipped = nil
    }

    local RegisterAttack = SafeWaitForChild(WaitChilds(ReplicatedStorage, "Modules", "Net"), "RE/RegisterAttack")
    local RegisterHit = SafeWaitForChild(WaitChilds(ReplicatedStorage, "Modules", "Net"), "RE/RegisterHit")

    local function IsAlive(character)
        return character and character:FindFirstChild("Humanoid") and character.Humanoid.Health > 0
    end

    local function ProcessEnemies(OthersEnemies, Folder)
        local BasePart = nil
        for _, Enemy in Folder:GetChildren() do
            local Head = Enemy:FindFirstChild("Head")
            if Head and IsAlive(Enemy) and Player:DistanceFromCharacter(Head.Position) < FastAttack.Distance then
                if Enemy ~= Player.Character then
                    table.insert(OthersEnemies, { Enemy, Head })
                    BasePart = Head
                end
            end
        end
        return BasePart
    end

    function FastAttack:Attack(BasePart, OthersEnemies)
        if not BasePart or #OthersEnemies == 0 then return end
        if RegisterAttack and RegisterHit then
            RegisterAttack:FireServer(FastAttackSettings.ClickDelay or 0)
            RegisterHit:FireServer(BasePart, OthersEnemies)
        end
    end

    function FastAttack:AttackNearest()
        local OthersEnemies = {}
        local Enemies = workspace:FindFirstChild("Enemies")
        local Characters = workspace:FindFirstChild("Characters")
        
        if not Enemies or not Characters then return end
        
        local Part1 = ProcessEnemies(OthersEnemies, Enemies)
        local Part2 = ProcessEnemies(OthersEnemies, Characters)
        if #OthersEnemies > 0 then
            self:Attack(Part1 or Part2, OthersEnemies)
        else
            task.wait(0)
        end
    end

    function FastAttack:BladeHits()
        if not Player.Character then return end
        local Equipped = IsAlive(Player.Character) and Player.Character:FindFirstChildOfClass("Tool")
        if Equipped and Equipped.ToolTip ~= "Gun" then
            self:AttackNearest()
        else
            task.wait(0)
        end
    end

    local battleLoop = nil
    
    function FastAttack:Start()
        if battleLoop then return end
        
        battleLoop = task.spawn(function()
            while FastAttackSettings.AutoClick do
                if Player and Player.Character then
                    self:BladeHits()
                end
                task.wait(FastAttackSettings.ClickDelay)
            end
            battleLoop = nil
        end)
    end
    
    function FastAttack:Stop()
        FastAttackSettings.AutoClick = false
        if battleLoop then
            task.cancel(battleLoop)
            battleLoop = nil
        end
    end

    getgenv().rz_FastAttack = FastAttack
    return FastAttack
end)()

-- 自動選擇隊伍功能
local function setupAutoTeam()
    task.spawn(function()
        while task.wait(1) do
            if not FastAttackSettings.AutoClick then break end
            
            local chooseTeam = PlayerGui:FindFirstChild("ChooseTeam", true)
            local uiController = PlayerGui:FindFirstChild("UIController", true)

            if chooseTeam and chooseTeam.Visible and uiController then
                for _, v in pairs(getgc(true)) do
                    if type(v) == "function" and getfenv(v).script == uiController then
                        local constant = getconstants(v)
                        pcall(function()
                            if (constant[1] == "Pirates" or constant[1] == "Marines") and #constant == 1 then
                                if constant[1] == FastAttackSettings.Team then
                                    v(FastAttackSettings.Team)
                                end
                            end
                        end)
                    end
                end
            end
        end
    end)
end
-- ========== 自动战斗功能代码结束 ==========

-- 重生成海盜按鈕事件
respawnPirateButton.MouseButton1Click:Connect(function()
    FastAttackSettings.Team = "Pirates"
    pcall(function()
        ReplicatedStorage.Remotes.CommF_:InvokeServer("SetTeam", "Pirates")
    end)
    game.StarterGui:SetCore("SendNotification", {
        Title = "陣營",
        Text = "已切換為海盜",
        Duration = 3
    })
end)

-- 重生成海軍按鈕事件
respawnMarineButton.MouseButton1Click:Connect(function()
    FastAttackSettings.Team = "Marines"
    pcall(function()
        ReplicatedStorage.Remotes.CommF_:InvokeServer("SetTeam", "Marines")
    end)
    game.StarterGui:SetCore("SendNotification", {
        Title = "陣營",
        Text = "已切換為海軍",
        Duration = 3
    })
end)

-- 自動戰鬥開關按鈕事件
toggleAutoBattleButton.MouseButton1Click:Connect(function()
    FastAttackSettings.AutoClick = not FastAttackSettings.AutoClick
    
    if FastAttackSettings.AutoClick then
        toggleAutoBattleButton.BackgroundColor3 = Color3.fromRGB(80, 200, 120)
        toggleAutoBattleButton.Text = "開啟自動攻擊"
        if FastAttackModule.FastAttack then
            FastAttackModule.FastAttack:Start()
        end
        setupAutoTeam()
        game.StarterGui:SetCore("SendNotification", {
            Title = "自動戰鬥",
            Text = "自動戰鬥已開啟",
            Duration = 3
        })
    else
        toggleAutoBattleButton.BackgroundColor3 = Color3.fromRGB(200, 80, 80)
        toggleAutoBattleButton.Text = "關閉自動攻擊"
        if FastAttackModule.FastAttack then
            FastAttackModule.FastAttack:Stop()
        end
        game.StarterGui:SetCore("SendNotification", {
            Title = "自動戰鬥",
            Text = "自動戰鬥已關閉",
            Duration = 3
        })
    end
end)

-- ========== 速度控制功能代碼開始 ==========
local TweenService = game:GetService("TweenService")
local speedConnection = nil
local currentSpeedValue = nil

local function startSpeedLoop(val)
    if speedConnection then
        speedConnection:Disconnect()
        speedConnection = nil
    end
    currentSpeedValue = val
    speedConnection = RunService.Heartbeat:Connect(function()
        local char = Player.Character
        if not char then return end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        local hum = char:FindFirstChildOfClass("Humanoid")
        if not hrp or not hum then return end
        local moveDir = hum.MoveDirection
        if moveDir.Magnitude > 0.1 then
            local distance = currentSpeedValue * 0.05
            local tweenInfo = TweenInfo.new(0.05, Enum.EasingStyle.Linear)
            local targetCFrame = CFrame.new(hrp.Position + moveDir * distance) * (hrp.CFrame - hrp.Position)
            TweenService:Create(hrp, tweenInfo, {CFrame = targetCFrame}):Play()
        end
    end)
end

Player.CharacterAdded:Connect(function()
    if currentSpeedValue then
        task.wait(0.5)
        startSpeedLoop(currentSpeedValue)
    end
end)

confirmSpeedButton.MouseButton1Click:Connect(function()
    local inputVal = tonumber(speedTextBox.Text)
    if not inputVal then
        game.StarterGui:SetCore("SendNotification", {
            Title = "速度設置",
            Text = "設定失敗，請輸入數字",
            Duration = 3
        })
        return
    end
    startSpeedLoop(inputVal)
    speedStatusLabel.Text = "目前速度設定: " .. inputVal
end)
-- ========== 速度控制功能代碼結束 ==========

-- ========== 自由飛行功能代碼開始（完整相機視角版） ==========
local UserInputService = game:GetService("UserInputService")
local ControlModule = nil
pcall(function()
    ControlModule = require(Player.PlayerScripts:WaitForChild("PlayerModule")):GetControls()
end)

-- 顯示「目前狀態」，避免按鈕文字與顏色被理解為相反含義。
toggleFlyButton.Text = "自由飛行：已關閉"
toggleFlyButton.BackgroundColor3 = Color3.fromRGB(200, 80, 80)

local function clearFlyObjects(root)
    if not root then return end
    local velocity = root:FindFirstChild("FlyVelocity")
    local gyro = root:FindFirstChild("FlyGyro")
    if velocity then velocity:Destroy() end
    if gyro then gyro:Destroy() end
end

local function getRawFlyInput(humanoid)
    local input = Vector3.new(0, 0, 0)
    if ControlModule then
        pcall(function() input = ControlModule:GetMoveVector() end)
    end
    -- 部分手機控制器在特殊狀態下不回傳 ControlModule 向量，使用原生移動方向作為備援。
    if input.Magnitude < 0.01 and humanoid then
        local worldMove = humanoid.MoveDirection
        if worldMove.Magnitude > 0.01 then
            local camera = workspace.CurrentCamera
            if camera then
                input = Vector3.new(worldMove:Dot(camera.CFrame.RightVector), 0, -worldMove:Dot(camera.CFrame.LookVector))
            end
        end
    end
    return input
end

local function stopFreeFly()
    isFlying = false
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end

    local character = Player.Character
    local root = character and character:FindFirstChild("HumanoidRootPart")
    local humanoid = character and character:FindFirstChildOfClass("Humanoid")
    if root then
        root.Anchored = false
        clearFlyObjects(root)
    end
    if humanoid then
        humanoid.PlatformStand = false
        humanoid.AutoRotate = true
    end
    toggleFlyButton.Text = "自由飛行：已關閉"
    toggleFlyButton.BackgroundColor3 = Color3.fromRGB(200, 80, 80)
end

local function startFreeFly()
    -- 防止重複連線；並在每次開啟時讀取目前角色與相機。
    if flyConnection then flyConnection:Disconnect(); flyConnection = nil end
    local character = Player.Character
    local humanoid = character and character:FindFirstChildOfClass("Humanoid")
    local root = character and character:FindFirstChild("HumanoidRootPart")
    if not humanoid or not root then
        isFlying = false
        return
    end

    clearFlyObjects(root)
    isFlying = true
    -- 坐船時保留角色與座位的連結，讓船模型帶著角色一起飛；未坐船才固定角色。
    root.Anchored = getSeatedVessel(character) == nil
    humanoid.PlatformStand = false
    humanoid.AutoRotate = false
    toggleFlyButton.Text = "自由飛行：已開啟"
    toggleFlyButton.BackgroundColor3 = Color3.fromRGB(80, 200, 120)

    flyConnection = RunService.RenderStepped:Connect(function(deltaTime)
        local currentCharacter = Player.Character
        local currentHumanoid = currentCharacter and currentCharacter:FindFirstChildOfClass("Humanoid")
        local currentRoot = currentCharacter and currentCharacter:FindFirstChild("HumanoidRootPart")
        local camera = workspace.CurrentCamera
        if not isFlying or not currentHumanoid or not currentRoot or not camera then
            stopFreeFly()
            return
        end

        local moveInput = getRawFlyInput(currentHumanoid)
        local forwardAmount = -moveInput.Z
        local sideAmount = moveInput.X

        -- 關鍵：前後方向直接使用相機完整 LookVector（包含 Y 軸）。
        -- 因此鏡頭指向天空時，前進會真正向天空位移；指向地面則向下位移。
        local desiredDirection = camera.CFrame.LookVector * forwardAmount + camera.CFrame.RightVector * sideAmount
        if desiredDirection.Magnitude > 0.01 then
            local movement = desiredDirection.Unit * flySpeed * deltaTime
            -- 坐船時移動船體，角色會與船座同步；未坐船才移動角色本身。
            local movedVessel = moveVesselOrCharacter(currentCharacter, currentRoot, movement, camera.CFrame.LookVector)
            currentRoot.Anchored = not movedVessel
        end
    end)
end

toggleFlyButton.MouseButton1Click:Connect(function()
    if isFlying then
        stopFreeFly()
    else
        startFreeFly()
        if isFlying then
            game.StarterGui:SetCore("SendNotification", {
                Title = "自由飛行已開啟",
                Text = "前進會沿鏡頭中心的完整方向飛行；看天空即向上飛行",
                Duration = 3
            })
        end
    end
end)

-- 速度滑動條控制邏輯（上限 400）
local isDraggingSlider = false
local function updateSlider(inputX)
    local sliderFrameAbsolutePositionX = flySpeedSliderFrame.AbsolutePosition.X
    local sliderFrameAbsoluteSizeX = math.max(flySpeedSliderFrame.AbsoluteSize.X, 1)
    local relativeX = math.clamp(inputX - sliderFrameAbsolutePositionX, 0, sliderFrameAbsoluteSizeX)
    local percentage = relativeX / sliderFrameAbsoluteSizeX
    flySpeed = math.floor(percentage * 390) + 10
    flySpeedLabel.Text = "飛行速度: " .. flySpeed
    flySpeedSlider.Size = UDim2.new(percentage, 0, 1, 0)
    flySpeedThumb.Position = UDim2.new(percentage, -5, -0.5, 0)
end

flySpeedSlider.MouseButton1Down:Connect(function() isDraggingSlider = true end)
flySpeedSlider.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.Touch then
        isDraggingSlider = true
        updateSlider(input.Position.X)
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then isDraggingSlider = false end
end)
UserInputService.InputChanged:Connect(function(input)
    if isDraggingSlider and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then updateSlider(input.Position.X) end
end)

flySettingsButton.MouseButton1Click:Connect(function()
    MainPanel.Visible = false
    InfoPanel.Visible = false
    BattlePanel.Visible = false
    SpeedPanel.Visible = false
    if ESPPanel then ESPPanel.Visible = false end
    FlyPanel.Visible = true
    updatePanelStatus()
end)
flyBackButton.MouseButton1Click:Connect(function()
    FlyPanel.Visible = false
    MainPanel.Visible = true
    updatePanelStatus()
end)
Player.CharacterAdded:Connect(function()
    if isFlying then task.wait(0.5); startFreeFly() end
end)
-- ========== 自由飛行功能代碼結束 ==========

-- ========== ESP 果實功能強化版 ==========
local espFruitEnabled = false
local fruitEspConnection = nil

local function createFruitEsp(v)
    if not v:FindFirstChild("FruitESP") then
        local billboard = Instance.new("BillboardGui")
        billboard.Name = "FruitESP"
        billboard.AlwaysOnTop = true
        billboard.Size = UDim2.new(0, 100, 0, 50)
        billboard.Adornee = v
        
        local lbl = Instance.new("TextLabel")
        lbl.Parent = billboard
        lbl.BackgroundTransparency = 1
        lbl.Size = UDim2.new(1, 0, 1, 0)
        lbl.Text = "🍎 " .. v.Name
        lbl.TextColor3 = Color3.fromRGB(255, 50, 50)
        lbl.TextStrokeTransparency = 0
        lbl.TextScaled = true
        lbl.Font = Enum.Font.GothamBold
        
        billboard.Parent = v
    end
end

local function updateFruitEsp()
    for _, v in pairs(workspace:GetChildren()) do
        -- 偵測所有 Tool (果實通常是 Tool) 或者名字包含 Fruit 的物體
        if v:IsA("Tool") or (v:IsA("Model") and string.find(v.Name:lower(), "fruit")) then
            -- 排除玩家背著的果實
            if not v:FindFirstChildOfClass("Humanoid") and not v.Parent:FindFirstChildOfClass("Humanoid") then
                createFruitEsp(v)
            end
        end
    end
end

espFruitButton.MouseButton1Click:Connect(function()
    espFruitEnabled = not espFruitEnabled
    if espFruitEnabled then
        espFruitButton.Text = "ESP 果實 開啟"
        espFruitButton.BackgroundColor3 = Color3.fromRGB(80, 200, 120)
        
        -- 立即執行一次
        updateFruitEsp()
        
        -- 開啟持續偵測
        fruitEspConnection = RunService.Heartbeat:Connect(function()
            updateFruitEsp()
        end)
        
        game.StarterGui:SetCore("SendNotification", {
            Title = "ESP 果實",
            Text = "果實透視已開啟",
            Duration = 3
        })
    else
        espFruitButton.Text = "ESP 果實 關閉"
        espFruitButton.BackgroundColor3 = Color3.fromRGB(220, 130, 50)
        
        if fruitEspConnection then
            fruitEspConnection:Disconnect()
            fruitEspConnection = nil
        end
        
        -- 清除所有標籤
        for _, v in pairs(workspace:GetDescendants()) do
            if v.Name == "FruitESP" then
                v:Destroy()
            end
        end
        
        game.StarterGui:SetCore("SendNotification", {
            Title = "ESP 果實",
            Text = "果實透視已關閉",
            Duration = 3
        })
    end
end)
-- ========== ESP 果實功能結束 ==========






-- ========== updatePanelStatus（在圖標事件之前定義）==========
local function updatePanelStatus()
    if MainPanel.Visible then
        IconButton.BackgroundColor3 = Color3.fromRGB(100, 255, 100)
        IconButton.BackgroundTransparency = 0
    elseif InfoPanel.Visible then
        IconButton.BackgroundColor3 = Color3.fromRGB(100, 100, 255)
        IconButton.BackgroundTransparency = 0
    elseif BattlePanel.Visible then
        IconButton.BackgroundColor3 = Color3.fromRGB(255, 150, 50)
        IconButton.BackgroundTransparency = 0
    elseif ESPPanel and ESPPanel.Visible then
        IconButton.BackgroundColor3 = Color3.fromRGB(180, 80, 200)
        IconButton.BackgroundTransparency = 0
    elseif FlyPanel.Visible then
        IconButton.BackgroundColor3 = Color3.fromRGB(150, 100, 255)
        IconButton.BackgroundTransparency = 0
    elseif SpeedPanel.Visible then
        IconButton.BackgroundColor3 = Color3.fromRGB(100, 255, 255)
        IconButton.BackgroundTransparency = 0
    else
        IconButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        IconButton.BackgroundTransparency = 0.5
    end
end

-- 圖標點擊事件
IconButton.MouseButton1Click:Connect(function()
    if MainPanel.Visible or InfoPanel.Visible or BattlePanel.Visible or SpeedPanel.Visible or FlyPanel.Visible or (ESPPanel and ESPPanel.Visible) then
        MainPanel.Visible = false
        InfoPanel.Visible = false
        BattlePanel.Visible = false
        SpeedPanel.Visible = false
        FlyPanel.Visible = false
        if ESPPanel then ESPPanel.Visible = false end
    else
        MainPanel.Visible = true
    end
    updatePanelStatus()
end)

-- ========== ESP 玩家設定功能 ==========
local espEnabled = false
local ESPSettings = {
    Level = false,
    Distance = false,
    Health = false,
    Order = {}
}
local espFolder = Instance.new("Folder")
espFolder.Name = "ESPFolder"
espFolder.Parent = workspace
local espConnections = {}

ESPPanel = Instance.new("Frame")
ESPPanel.Name = "ESPPanel"
ESPPanel.Size = UDim2.new(0, 300, 0, 390)
ESPPanel.Position = UDim2.new(0, 90, 0, 20)
ESPPanel.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ESPPanel.BackgroundTransparency = 0.1
ESPPanel.Visible = false
ESPPanel.Parent = ScreenGui
Instance.new("UICorner", ESPPanel).CornerRadius = UDim.new(0, 12)

local espPanelTitle = Instance.new("TextLabel")
espPanelTitle.Size = UDim2.new(0, 260, 0, 35)
espPanelTitle.Position = UDim2.new(0, 20, 0, 10)
espPanelTitle.BackgroundTransparency = 1
espPanelTitle.Text = "ESP 玩家設定"
espPanelTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
espPanelTitle.TextScaled = true
espPanelTitle.Font = Enum.Font.GothamBold
espPanelTitle.Parent = ESPPanel

local espToggleButton = Instance.new("TextButton")
espToggleButton.Size = UDim2.new(0, 260, 0, 40)
espToggleButton.Position = UDim2.new(0, 20, 0, 55)
espToggleButton.BackgroundColor3 = Color3.fromRGB(200, 80, 80)
espToggleButton.Text = "ESP 玩家：關閉"
espToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
espToggleButton.TextScaled = true
espToggleButton.Parent = ESPPanel
Instance.new("UICorner", espToggleButton).CornerRadius = UDim.new(0, 8)

local levelToggleButton = Instance.new("TextButton")
levelToggleButton.Size = UDim2.new(0, 260, 0, 40)
levelToggleButton.Position = UDim2.new(0, 20, 0, 105)
levelToggleButton.BackgroundColor3 = Color3.fromRGB(80, 110, 160)
levelToggleButton.Text = "顯示等級：關閉"
levelToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
levelToggleButton.TextScaled = true
levelToggleButton.Parent = ESPPanel
Instance.new("UICorner", levelToggleButton).CornerRadius = UDim.new(0, 8)

local distanceToggleButton = Instance.new("TextButton")
distanceToggleButton.Size = UDim2.new(0, 260, 0, 40)
distanceToggleButton.Position = UDim2.new(0, 20, 0, 155)
distanceToggleButton.BackgroundColor3 = Color3.fromRGB(80, 110, 160)
distanceToggleButton.Text = "顯示距離：關閉"
distanceToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
distanceToggleButton.TextScaled = true
distanceToggleButton.Parent = ESPPanel
Instance.new("UICorner", distanceToggleButton).CornerRadius = UDim.new(0, 8)

local healthToggleButton = Instance.new("TextButton")
healthToggleButton.Size = UDim2.new(0, 260, 0, 40)
healthToggleButton.Position = UDim2.new(0, 20, 0, 205)
healthToggleButton.BackgroundColor3 = Color3.fromRGB(80, 110, 160)
healthToggleButton.Text = "顯示血量：關閉"
healthToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
healthToggleButton.TextScaled = true
healthToggleButton.Parent = ESPPanel
Instance.new("UICorner", healthToggleButton).CornerRadius = UDim.new(0, 8)

local espOrderHint = Instance.new("TextLabel")
espOrderHint.Size = UDim2.new(0, 260, 0, 42)
espOrderHint.Position = UDim2.new(0, 20, 0, 255)
espOrderHint.BackgroundTransparency = 1
espOrderHint.Text = "名稱固定在最上方；其餘內容依開啟順序排列"
espOrderHint.TextColor3 = Color3.fromRGB(210, 210, 210)
espOrderHint.TextScaled = true
espOrderHint.TextWrapped = true
espOrderHint.Font = Enum.Font.Gotham
espOrderHint.Parent = ESPPanel

local espBackButton = Instance.new("TextButton")
espBackButton.Size = UDim2.new(0, 260, 0, 40)
espBackButton.Position = UDim2.new(0, 20, 0, 325)
espBackButton.BackgroundColor3 = Color3.fromRGB(200, 120, 80)
espBackButton.Text = "返回主頁"
espBackButton.TextColor3 = Color3.fromRGB(255, 255, 255)
espBackButton.TextScaled = true
espBackButton.Parent = ESPPanel
Instance.new("UICorner", espBackButton).CornerRadius = UDim.new(0, 8)

local function getPlayerLevel(targetPlayer)
    local ok, level = pcall(function() return targetPlayer.Data.Level.Value end)
    return (ok and level) and tostring(level) or "?"
end

local function getDistanceText(targetPlayer)
    local ownRoot = Player.Character and Player.Character:FindFirstChild("HumanoidRootPart")
    local targetRoot = targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not ownRoot or not targetRoot then return "距離：?" end
    return "距離：" .. tostring(math.floor((targetRoot.Position - ownRoot.Position).Magnitude + 0.5))
end

local function getHealthText(targetPlayer)
    local humanoid = targetPlayer.Character and targetPlayer.Character:FindFirstChildOfClass("Humanoid")
    if not humanoid then return "血量：?" end
    return "血量：" .. tostring(math.floor(humanoid.Health + 0.5)) .. "/" .. tostring(math.floor(humanoid.MaxHealth + 0.5))
end

local function renderESPLayout(targetPlayer, billboard)
    local content = billboard:FindFirstChild("Content")
    if not content then return end
    content:ClearAllChildren()

    local layout = Instance.new("UIListLayout")
    layout.Padding = UDim.new(0, 1)
    layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    layout.Parent = content

    local function addLine(name, text, color, bold)
        local line = Instance.new("TextLabel")
        line.Name = name
        line.Size = UDim2.new(1, 0, 0, 20)
        line.BackgroundTransparency = 1
        line.Text = text
        line.TextColor3 = color
        line.TextScaled = true
        line.Font = bold and Enum.Font.GothamBold or Enum.Font.Gotham
        line.TextStrokeTransparency = 0
        line.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
        line.Parent = content
        return line
    end

    addLine("NameLine", targetPlayer.Name, Color3.fromRGB(255, 255, 100), true)
    for _, key in ipairs(ESPSettings.Order) do
        if key == "Level" and ESPSettings.Level then
            addLine("LevelLine", "Lv. " .. getPlayerLevel(targetPlayer), Color3.fromRGB(100, 220, 255), false)
        elseif key == "Distance" and ESPSettings.Distance then
            addLine("DistanceLine", getDistanceText(targetPlayer), Color3.fromRGB(150, 255, 150), false)
        elseif key == "Health" and ESPSettings.Health then
            addLine("HealthLine", getHealthText(targetPlayer), Color3.fromRGB(255, 130, 130), false)
        end
    end

    billboard.Size = UDim2.new(0, 180, 0, 22 + (#ESPSettings.Order * 21))
end

local function updateESPValues(targetPlayer, billboard)
    local character = targetPlayer.Character
    local root = character and character:FindFirstChild("HumanoidRootPart")
    if root then billboard.Adornee = root end
    local content = billboard:FindFirstChild("Content")
    if not content then return end
    local levelLine = content:FindFirstChild("LevelLine")
    local distanceLine = content:FindFirstChild("DistanceLine")
    local healthLine = content:FindFirstChild("HealthLine")
    if levelLine then levelLine.Text = "Lv. " .. getPlayerLevel(targetPlayer) end
    if distanceLine then distanceLine.Text = getDistanceText(targetPlayer) end
    if healthLine then healthLine.Text = getHealthText(targetPlayer) end
end

local function createESP(targetPlayer)
    if targetPlayer == Player or espFolder:FindFirstChild(targetPlayer.Name) then return end
    local billboard = Instance.new("BillboardGui")
    billboard.Name = targetPlayer.Name
    billboard.AlwaysOnTop = true
    billboard.StudsOffset = Vector3.new(0, 3, 0)
    billboard.Parent = espFolder

    local content = Instance.new("Frame")
    content.Name = "Content"
    content.Size = UDim2.new(1, 0, 1, 0)
    content.BackgroundTransparency = 1
    content.Parent = billboard
    renderESPLayout(targetPlayer, billboard)
    updateESPValues(targetPlayer, billboard)

    espConnections[targetPlayer] = RunService.Heartbeat:Connect(function()
        if not espEnabled or not targetPlayer.Parent or not billboard.Parent then
            if espConnections[targetPlayer] then espConnections[targetPlayer]:Disconnect(); espConnections[targetPlayer] = nil end
            if billboard.Parent then billboard:Destroy() end
            return
        end
        updateESPValues(targetPlayer, billboard)
    end)
end

local function clearAllESP()
    for targetPlayer, connection in pairs(espConnections) do
        connection:Disconnect()
        espConnections[targetPlayer] = nil
    end
    for _, billboard in ipairs(espFolder:GetChildren()) do billboard:Destroy() end
end

local function buildAllESP()
    for _, targetPlayer in ipairs(Players:GetPlayers()) do createESP(targetPlayer) end
end

local function updateAllESPLayouts()
    if not espEnabled then return end
    for _, targetPlayer in ipairs(Players:GetPlayers()) do
        local billboard = espFolder:FindFirstChild(targetPlayer.Name)
        if billboard then renderESPLayout(targetPlayer, billboard); updateESPValues(targetPlayer, billboard) end
    end
end

local function updateExtraButton(button, label, enabled)
    button.Text = label .. (enabled and "：開啟" or "：關閉")
    button.BackgroundColor3 = enabled and Color3.fromRGB(80, 200, 120) or Color3.fromRGB(80, 110, 160)
end

local function toggleExtra(key, button, label)
    ESPSettings[key] = not ESPSettings[key]
    if ESPSettings[key] then
        local exists = false
        for _, value in ipairs(ESPSettings.Order) do if value == key then exists = true break end end
        if not exists then table.insert(ESPSettings.Order, key) end
    else
        for index, value in ipairs(ESPSettings.Order) do
            if value == key then table.remove(ESPSettings.Order, index); break end
        end
    end
    updateExtraButton(button, label, ESPSettings[key])
    updateAllESPLayouts()
end

espButton.MouseButton1Click:Connect(function()
    MainPanel.Visible = false
    InfoPanel.Visible = false
    BattlePanel.Visible = false
    SpeedPanel.Visible = false
    FlyPanel.Visible = false
    ESPPanel.Visible = true
    updatePanelStatus()
end)

espToggleButton.MouseButton1Click:Connect(function()
    espEnabled = not espEnabled
    if espEnabled then
        espToggleButton.Text = "ESP 玩家：開啟"
        espToggleButton.BackgroundColor3 = Color3.fromRGB(80, 200, 120)
        buildAllESP()
    else
        espToggleButton.Text = "ESP 玩家：關閉"
        espToggleButton.BackgroundColor3 = Color3.fromRGB(200, 80, 80)
        clearAllESP()
    end
end)
levelToggleButton.MouseButton1Click:Connect(function() toggleExtra("Level", levelToggleButton, "顯示等級") end)
distanceToggleButton.MouseButton1Click:Connect(function() toggleExtra("Distance", distanceToggleButton, "顯示距離") end)
healthToggleButton.MouseButton1Click:Connect(function() toggleExtra("Health", healthToggleButton, "顯示血量") end)
espBackButton.MouseButton1Click:Connect(function()
    ESPPanel.Visible = false
    MainPanel.Visible = true
    updatePanelStatus()
end)

Players.PlayerAdded:Connect(function(targetPlayer)
    if espEnabled then createESP(targetPlayer) end
end)
Players.PlayerRemoving:Connect(function(targetPlayer)
    local connection = espConnections[targetPlayer]
    if connection then connection:Disconnect(); espConnections[targetPlayer] = nil end
    local billboard = espFolder:FindFirstChild(targetPlayer.Name)
    if billboard then billboard:Destroy() end
end)
-- ========== ESP 玩家功能結束 ==========

-- ========== ESP 果實功能 ==========

local fruitEspFolder = Instance.new("Folder")
fruitEspFolder.Name = "FruitESPFolder"
fruitEspFolder.Parent = workspace
local fruitEspConnection = nil

local function scanFruits()
    for _, v in ipairs(fruitEspFolder:GetChildren()) do v:Destroy() end
    local containers = {"Fruits", "DroppedFruits", "MapItems"}
    for _, containerName in ipairs(containers) do
        local container = workspace:FindFirstChild(containerName)
        if container then
            for _, fruit in ipairs(container:GetChildren()) do
                local part = fruit:IsA("BasePart") and fruit
                    or (fruit:IsA("Model") and (fruit.PrimaryPart or fruit:FindFirstChildOfClass("BasePart")))
                if part then
                    local bb = Instance.new("BillboardGui")
                    bb.Name = fruit.Name
                    bb.Size = UDim2.new(0, 140, 0, 40)
                    bb.StudsOffset = Vector3.new(0, 4, 0)
                    bb.AlwaysOnTop = true
                    bb.Adornee = part
                    bb.Parent = fruitEspFolder
                    local lbl = Instance.new("TextLabel")
                    lbl.Size = UDim2.new(1, 0, 1, 0)
                    lbl.BackgroundTransparency = 1
                    lbl.Text = "🍎 " .. fruit.Name
                    lbl.TextColor3 = Color3.fromRGB(255, 220, 50)
                    lbl.TextScaled = true
                    lbl.Font = Enum.Font.GothamBold
                    lbl.TextStrokeTransparency = 0
                    lbl.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
                    lbl.Parent = bb
                end
            end
        end
    end
end


-- ========== ESP 果實功能結束 ==========

-- 初始更新面板狀態
updatePanelStatus()

-- ========== 飛到Tiki功能代碼開始 ==========
-- 飛行控制變量（飛到Tiki）
local isFlyingToTiki = false
local flyConnection = nil
local targetX = -16542  -- Tiki島X座標
local targetZ = 1044    -- Tiki島Z座標
local initialY = 500    -- 初始飛行高度
local finalY = 800      -- 最終飛行高度
local targetY = initialY -- 當前目標高度
local flightPhase = "ascending" -- 飛行階段: "ascending", "waiting", "ascendingTo800", "horizontal", "descending"
local waitTimer = 0

-- 檢查是否到達目標位置
local function hasReachedTarget(currentPos)
    local tolerance = 5  -- 容錯範圍
    return math.abs(currentPos.X - targetX) < tolerance and math.abs(currentPos.Z - targetZ) < tolerance
end

-- 檢測下方是否有地面
local function findGroundBelow(position)
    local rayOrigin = Vector3.new(position.X, position.Y + 10, position.Z)
    local rayDirection = Vector3.new(0, -1000, 0)
    local raycastParams = RaycastParams.new()
    raycastParams.FilterType = Enum.RaycastFilterType.Blacklist
    
    if Player.Character then
        raycastParams.FilterDescendantsInstances = {Player.Character}
    end
    
    local raycastResult = workspace:Raycast(rayOrigin, rayDirection, raycastParams)
    
    if raycastResult then
        return raycastResult.Position.Y + 5  -- 在地面位置上方5單位
    end
    
    return position.Y - 100  -- 如果沒檢測到，下降100單位
end

-- 飛行功能
local function startFlyToTiki()
    if not Player.Character then 
        warn("沒有找到玩家角色")
        return 
    end
    
    local humanoidRootPart = Player.Character:FindFirstChild("HumanoidRootPart")
    if not humanoidRootPart then 
        warn("沒有找到 HumanoidRootPart")
        return 
    end
    
    -- 如果正在飛行，先停止
    if isFlyingToTiki and flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    
    -- 重置飛行狀態
    isFlyingToTiki = true
    flightPhase = "ascending"
    targetY = initialY
    waitTimer = 0
    
    -- 顯示開始通知
    game.StarterGui:SetCore("SendNotification", {
        Title = "飛往 Tiki 島",
        Text = "正在起飛到高度 500...",
        Duration = 3
    })
    
    local currentPos = humanoidRootPart.Position
    
    -- 第一步：立即上升到 Y=500
    humanoidRootPart.CFrame = CFrame.new(currentPos.X, targetY, currentPos.Z)
    
    -- 飛行循環
    flyConnection = RunService.Heartbeat:Connect(function(deltaTime)
        if not isFlyingToTiki or not Player.Character then
            if flyConnection then
                flyConnection:Disconnect()
                flyConnection = nil
            end
            return
        end
        
        local hrp = Player.Character:FindFirstChild("HumanoidRootPart")
        if not hrp then
            if flyConnection then
                flyConnection:Disconnect()
                flyConnection = nil
            end
            return
        end
        
        local currentPos = hrp.Position
        
        -- 飛行階段控制
        if flightPhase == "ascending" then
            -- 已經在500高度，切換到等待階段
            flightPhase = "waiting"
            
            game.StarterGui:SetCore("SendNotification", {
                Title = "等待中",
                Text = "在高度 500 等待 1 秒...",
                Duration = 1
            })
            
        elseif flightPhase == "waiting" then
            -- 等待1秒階段
            waitTimer = waitTimer + deltaTime
            
            -- 保持在500高度
            hrp.CFrame = CFrame.new(currentPos.X, initialY, currentPos.Z)
            
            if waitTimer >= 1 then
                -- 等待結束，切換到上升到800階段
                flightPhase = "ascendingTo800"
                targetY = finalY
                
                game.StarterGui:SetCore("SendNotification", {
                    Title = "上升中",
                    Text = "正在上升到高度 800...",
                    Duration = 2
                })
            end
            
        elseif flightPhase == "ascendingTo800" then
            -- 上升到800高度
            hrp.CFrame = CFrame.new(currentPos.X, finalY, currentPos.Z)
            
            -- 切換到水平飛行階段
            flightPhase = "horizontal"
            
        elseif flightPhase == "horizontal" then
            -- 水平飛行階段
            if hasReachedTarget(currentPos) then
                -- 到達目標位置，開始下降
                flightPhase = "descending"
                
                game.StarterGui:SetCore("SendNotification", {
                    Title = "到達 Tiki 島",
                    Text = "正在下降尋找地面...",
                    Duration = 3
                })
            else
                -- 計算朝向目標的方向
                local direction = Vector3.new(targetX, targetY, targetZ) - currentPos
                local horizontalDirection = Vector3.new(direction.X, 0, direction.Z)
                
                if horizontalDirection.Magnitude > 0 then
                    -- 標準化方向並計算移動距離
                    horizontalDirection = horizontalDirection.Unit
                    local moveDistance = 300 * deltaTime
                    
                    -- 如果剩餘距離小於移動距離，直接跳到目標
                    if horizontalDirection.Magnitude * moveDistance > direction.Magnitude then
                        hrp.CFrame = CFrame.new(targetX, targetY, targetZ)
                    else
                        -- 計算新位置
                        local newX = currentPos.X + horizontalDirection.X * moveDistance
                        local newZ = currentPos.Z + horizontalDirection.Z * moveDistance
                        
                        -- 移動到新位置（保持高度targetY）
                        hrp.CFrame = CFrame.new(newX, targetY, newZ)
                    end
                end
            end
            
        elseif flightPhase == "descending" then
            -- 下降階段
            local groundY = findGroundBelow(currentPos)
            
            if math.abs(currentPos.Y - groundY) < 2 then
                -- 已經接近地面，停止下降
                hrp.CFrame = CFrame.new(currentPos.X, groundY, currentPos.Z)
                isFlyingToTiki = false
                
                if flyConnection then
                    flyConnection:Disconnect()
                    flyConnection = nil
                end
                
                game.StarterGui:SetCore("SendNotification", {
                    Title = "成功著陸",
                    Text = "已到達 Tiki 島地面",
                    Duration = 3
                })
            else
                -- 繼續下降
                local descendSpeed = 100  -- 下降速度
                local newY = currentPos.Y - descendSpeed * deltaTime
                
                if newY < groundY then
                    newY = groundY
                end
                
                hrp.CFrame = CFrame.new(currentPos.X, newY, currentPos.Z)
            end
        end
    end)
end
-- ========== 飛到Tiki功能代碼結束 ==========

-- 飛到Tiki按鈕的點擊事件
flyToTikiButton.MouseButton1Click:Connect(function()
    startFlyToTiki()
    
    game.StarterGui:SetCore("SendNotification", {
        Title = "提示",
        Text = "正在飛往 Tiki 島...",
        Duration = 3
    })
end)

-- ========== ESP 功能代碼開始 ==========
local espEnabled = false
local espFolder = Instance.new("Folder")
espFolder.Name = "ESPFolder"
espFolder.Parent = workspace

local camera = workspace.CurrentCamera

-- 取得玩家等級
local function getPlayerLevel(targetPlayer)
    local ok, level = pcall(function()
        return targetPlayer.Data.Level.Value
    end)
    if ok and level then
        return tostring(level)
    end
    return "?"
end

-- 幫一個玩家建立 ESP 標籤
local function createESP(targetPlayer)
    -- 避免重複建立
    if espFolder:FindFirstChild(targetPlayer.Name) then return end

    local billboard = Instance.new("BillboardGui")
    billboard.Name = targetPlayer.Name
    billboard.Size = UDim2.new(0, 160, 0, 50)
    billboard.StudsOffset = Vector3.new(0, 3, 0)  -- 頭頂上方
    billboard.AlwaysOnTop = true                   -- 穿牆也看得到
    billboard.Enabled = true
    billboard.Parent = espFolder

    -- 名字標籤
    local nameLabel = Instance.new("TextLabel")
    nameLabel.Name = "NameLabel"
    nameLabel.Size = UDim2.new(1, 0, 0.5, 0)
    nameLabel.Position = UDim2.new(0, 0, 0, 0)
    nameLabel.BackgroundTransparency = 1
    nameLabel.TextColor3 = Color3.fromRGB(255, 255, 100)  -- 黃色
    nameLabel.TextScaled = true
    nameLabel.Font = Enum.Font.GothamBold
    nameLabel.Text = targetPlayer.Name
    nameLabel.TextStrokeTransparency = 0  -- 黑色描邊，更好讀
    nameLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    nameLabel.Parent = billboard

    -- 等級標籤
    local levelLabel = Instance.new("TextLabel")
    levelLabel.Name = "LevelLabel"
    levelLabel.Size = UDim2.new(1, 0, 0.5, 0)
    levelLabel.Position = UDim2.new(0, 0, 0.5, 0)
    levelLabel.BackgroundTransparency = 1
    levelLabel.TextColor3 = Color3.fromRGB(100, 220, 255)  -- 淡藍色
    levelLabel.TextScaled = true
    levelLabel.Font = Enum.Font.Gotham
    levelLabel.Text = "Lv. " .. getPlayerLevel(targetPlayer)
    levelLabel.TextStrokeTransparency = 0
    levelLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    levelLabel.Parent = billboard

    -- 持續更新等級和位置
    local updateConnection
    updateConnection = RunService.Heartbeat:Connect(function()
        -- 如果 ESP 被關閉，或玩家離開，清除
        if not espEnabled or not targetPlayer or not targetPlayer.Parent then
            billboard:Destroy()
            updateConnection:Disconnect()
            return
        end

        local char = targetPlayer.Character
        if char then
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hrp then
                billboard.Adornee = hrp
            end
        end

        -- 每 3 秒更新一次等級（避免每幀都讀）
        levelLabel.Text = "Lv. " .. getPlayerLevel(targetPlayer)
    end)
end

-- 移除所有 ESP
local function clearAllESP()
    for _, child in ipairs(espFolder:GetChildren()) do
        child:Destroy()
    end
end

-- 對所有目前玩家建立 ESP
local function buildAllESP()
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= Player then  -- 不顯示自己
            createESP(p)
        end
    end
end

-- 有新玩家加入時自動建立
Players.PlayerAdded:Connect(function(p)
    if espEnabled then
        createESP(p)
    end
end)

-- 玩家離開時清除（billboard 的 updateConnection 內部會自動清除）

-- ========== ESP 功能代碼結束 ==========
